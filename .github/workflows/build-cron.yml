name: build-cron
on:
  schedule:
    - cron: "0 5 * * *"
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: hit build endpoint
        run: node ./scripts/exponential-retry-back-off.js
